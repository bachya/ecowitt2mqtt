apiVersion: v1
kind: Service
metadata:
  name: {{ include "ecowitt2mqtt.fullname" . }}
  labels:
    {{- include "ecowitt2mqtt.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ecowitt2mqtt.selectorLabels" . | nindent 4 }}
